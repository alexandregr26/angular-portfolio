export interface CommunityBox {
    img: string;
    title: string;
    titleKey?: string;
    date: string;
    dateKey?: string;
    description: string;
    descriptionKey?: string;
    bottomPriority?: boolean;
}
